# FitFindr — Starter Kit

This starter kit contains everything you need to begin Project 2.

## What's Included

```
ai201-project2-fitfindr-starter/
├── data/
│   ├── listings.json          # 40 mock secondhand listings
│   └── wardrobe_schema.json   # Wardrobe format + example wardrobe
├── utils/
│   └── data_loader.py         # Helper functions for loading the data
├── planning.md                # Your planning template — fill this out first
└── requirements.txt           # Python dependencies
```

## Setup

```bash
pip install -r requirements.txt
```

Set your Groq API key in a `.env` file (get a free key at [console.groq.com](https://console.groq.com)):
```
GROQ_API_KEY=your_key_here
```

## The Mock Listings Dataset

`data/listings.json` contains 40 mock secondhand listings across categories (tops, bottoms, outerwear, shoes, accessories) and styles (vintage, y2k, grunge, cottagecore, streetwear, and more).

Each listing has: `id`, `title`, `description`, `category`, `style_tags`, `size`, `condition`, `price`, `colors`, `brand`, and `platform`.

Load it with:
```python
from utils.data_loader import load_listings
listings = load_listings()
```

## The Wardrobe Schema

`data/wardrobe_schema.json` defines the format your agent uses to represent a user's existing wardrobe. It includes:

- `schema`: field definitions for a wardrobe item
- `example_wardrobe`: a sample wardrobe with 10 items you can use for testing
- `empty_wardrobe`: a starting template for a new user

Load an example wardrobe with:
```python
from utils.data_loader import get_example_wardrobe
wardrobe = get_example_wardrobe()
```

## Where to Start

1. **Read `planning.md` and fill it out before writing any code.**
2. Verify the data loads correctly by running `python utils/data_loader.py`.
3. Build and test each tool individually before connecting them through your planning loop.

Your implementation files go in this same directory. There's no required file structure for your agent code — organize it however makes sense for your design.

## Tool Inventory

### Tool 1: search_listings

**What it does:**
<!-- Describe what this tool does in 1–2 sentences -->
This tool will take in info about a piece of clothing the user is looking for including its description, size, and max_price and using this information it will look in `listings.json` for items that match

**Input parameters:**
<!-- List each parameter, its type, and what it represents -->
- `description` (str): A short description of the clothing piece 
- `size` (str): The size of the piece the user is looking for
- `max_price` (float): A maximum price the user is willing to pay

**What it returns:**
<!-- Describe the return value — what fields does a result contain? -->
Returns a list matching listings, sorted by relevance (best match first).

---

### Tool 2: suggest_outfit

**What it does:**
<!-- Describe what this tool does in 1–2 sentences -->
This tool will take in an item that was returned by Tool 1 `search_listings` and the user's current wardrobe and use the Groq API to suggest a cohesive, stylized outfit recommendation.

**Input parameters:**
<!-- List each parameter, its type, and what it represents -->
- `new_item` (dict): The new item received from Tool 1 that the user would consider buying
- `wardrobe` (dict): The user's items in their current wardrobe

**What it returns:**
<!-- Describe the return value -->
Returns a string generated by Groq suggesting what item to pair the new item with from the user's existing closet, including brief styling tips.

---

### Tool 3: create_fit_card

**What it does:**
<!-- Describe what this tool does in 1–2 sentences -->
This tool will take in the result from Tool 2 `suggest_outfit` and pass it to the Groq API with a specific system prompt to create an engaging social media caption that gives off a casual Instagram vibe.

**Input parameters:**
<!-- List each parameter, its type, and what it represents -->
- `outfit` (str): The outfit suggestion generated by Tool 2 
- `new_item` (dict): The new item generated by Tool 1

**What it returns:**
<!-- Describe the return value -->
Returns a string generated by Groq that is 2-4 sentences long, written in a casual social media tone with emojis, describing the suggested outfit.

## Planning Loop
1. **Initialization** 
     - Start by calling `new_session(query, wardrobe)` to create the session dictionary.

2. **Parsing Phase**
     - Extract `description`, `size`, and `max_price` from `session["query"]`.
     - Store these in `session["parsed"]` (e.g., as `{"description": "...", "size": "...", "max_price": ...}`).
     - **Parsing approach:** The query is parsed using the Groq LLM (`llama-3.3-70b-versatile`) with `temperature=0`. The LLM is prompted to return a JSON object with exactly three keys: `description`, `size`, and `max_price`. Temperature is set to 0 because parsing requires deterministic fact extraction — the same query should always produce the same parameters. Higher temperatures would introduce randomness and could cause inconsistent search results across runs.

3. **Search Phase (Tool 1)**
     - Call `search_listings(description=session["parsed"]["description"], size=session["parsed"]["size"], max_price=session["parsed"]["max_price"])`.
     - Store the returned list of dictionaries in `session["search_results"]`.

4. **Condition Check 1 (Search Results Check):**
     - IF `session["search_results"]` is empty (length if 0):
          - Set `session["error"]` to "No matching listings."
          - Return `session` immediately and terminate the loop.
     - ELSE (results exist):
          - Set `session["selected_item"] = session["search_results"][0]` (the top/first listing)
          - Proceed to the next step.

5. **Outfit Phase (Tool 2):**
     - Call `suggest_outfit(new_item=session["selected_item"], wardrobe=session["wardrobe"])`.
     - Store the returned string in `session["outfit_suggestion"]`.

6. **Condition Check 2 (Outfit Recommendation Check):**
     - IF `session["outfit_suggestion"]` is empty or `None`:
          - Set `session["error"]` to "Failed to generate outfit suggestion."
          - Return `session` immediately and terminate the loop.
     - ELSE:
          - Proceed to the next step.

7. **Fit Card Phase (Tool 3):**
     - Call `create_fit_card(outfit=session["outfit_suggestion"], new_item=session["selected_item"])`.

8. **Condition Check 3 (Fit Card Check):**
     - IF `session["fit_card"]` is empty or `None`:
          - Set `session["error"]` to "Failed to generate fit card."
          - Return `session` immediately.
     - ELSE:
          - Return `session` (success!).

## State Management Approach
The agent manages state using a centralized mutable `session` dictionary created at initialization.

Data is propagated through the workflow sequentially using these tracked keys:
- `session["query"]`: Stores the original user text string.
- `session["wardrobe"]`: Stores the user's initial closet items dictionary.
- `session["parsed"]`: Tracks structural constraints extracted from raw query text.
- `session["search_results"]`: Tracks lists of items matched by Tool 1.
- `session["selected_item"]`: Points specifically to index 0 of our search results to feed Tool 2.
- `session["outfit_suggestion"]`: Captures the text string returned by Tool 2.
- `session["fit_card"]`: Stores the final social media text block returned by Tool 3.
- `session["error"]`: Tracks any early termination error messages.


## Error Handling Per Tool
| Tool | Failure mode | Agent response |
|------|--------------|----------------|
| search_listings | No results match the query | Stops loop execution early, saves 'No matching listings.' into `session["error"]`, and exits back to user. |
| suggest_outfit | Wardrobe is empty | Stops loop execution early, saves 'Failed to generate outfit suggestion.' into `session["error"]`, and exits back to user. |
| suggest_outfit | No new item | Stops loop execution early, saves 'Failed to generate outfit suggestion.' into `session["error"]`, and exits back to user. |
| create_fit_card | Outfit input is missing or incomplete | Stops loop execution early, saves 'Failed to generate fit card.' into `session["error"]`, and exits back to user. |


## Spec Reflection 

**One way the spec helped you during implementation:** The spec was very helpful in planning out each step of the process in putting together this agent. A helpful part was planning what each tool does and exactly what parameters it will take in and what it should return. It was also helpful writing up my AI Tool Plan and what I need to check over and what I will test to see if my AI coding assistant provided correct results.

**One way your implementation diverged from the spec, and why:** This time I took more time in putting together the spec and making sure everything was correct that I wanted to hand over to my coding assistant and prompting went really smoothly so nothing diverged.

## AI Usage

I used Claude Code throughout the project.

**Instance 1 — Implementing `suggest_outfit` (Tool 2)**

- *What I gave the AI:* The Tool 2 spec from `planning.md` (inputs, return value, empty wardrobe fallback) and a sample of `wardrobe_schema.json`.
- *What it produced:* A `suggest_outfit()` function that builds a formatted prompt from the new item and wardrobe, branches into a general styling advice prompt when the wardrobe is empty, and calls Groq to return a single outfit suggestion in 3–5 sentences.
- *What I changed or overrode:* I overrode the model from `llama3-8b-8192` to `llama-3.3-70b-versatile`. I also asked Claude to add `category`, `brand`, `style_tags`, and `notes` to the prompt context after reviewing the initial output, and to constrain the response to one outfit rather than two.

**Instance 2 — Implementing `run_agent()` (Planning Loop)**

- *What I gave the AI:* The full Planning Loop section (steps 1–8), the State Management schema, the Error Handling table, and the architecture diagram from `planning.md`.
- *What it produced:* A complete `run_agent()` implementation that uses Groq at `temperature=0` to parse the query into JSON, calls all three tools sequentially, checks for failures at each step, and returns the session dict.
- *What I changed or overrode:* The LLM was returning JSON wrapped in markdown code fences which caused a `JSONDecodeError`. I had Claude add a stripping step to handle that. I also asked Claude to add and then remove debug print statements to verify state was passing correctly between tools.